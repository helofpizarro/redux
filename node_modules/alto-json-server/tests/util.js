const stripUUID = obj => {
    const {id, ...rest} = obj;
    return rest;
};

const stripListUUID = objList => {
    return objList.map(obj => {
        const {id, ...rest} = obj;
        return rest;
    });

};

module.exports = {stripListUUID, stripUUID};
